import 'package:flutter/material.dart';
class Chip_Slots extends StatelessWidget {
  const Chip_Slots({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}